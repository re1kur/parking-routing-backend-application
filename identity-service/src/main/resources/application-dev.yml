spring:
  application:
    name: dev-id-service
  docker:
    compose:
      enabled: true
      file: identity-service/dev-compose.yml
  devtools:
    add-properties: true
  liquibase:
    change-log: db/changelog/db.changelog-master-dev.yml
  rabbitmq:
    password: developer
    username: developer
    virtual-host: /
logging:
  level:
    web: debug
