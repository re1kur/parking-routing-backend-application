spring:
  application:
    name: dev-id-service
  docker:
    compose:
      enabled: true
      file: identity-service/dev-compose.yml
  devtools:
    add-properties: true
  liquibase:
    change-log: db/changelog/db.changelog-master-dev.yml
logging:
  level:
    web: debug
