spring:
  docker:
    compose:
      enabled: true
      file: authz-service/dev-compose.yml
  devtools:
    add-properties: true
  rabbitmq:
    password: developer
    username: developer
    virtual-host: /

client:
  identity-service:
    login-endpoint: http://localhost:8081/api/auth/login

logging:
  level:
    web: debug